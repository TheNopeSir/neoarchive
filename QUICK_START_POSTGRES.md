# ⚡ Быстрый старт: Переход с Supabase на PostgreSQL

## 🎯 3 шага до свободы от Supabase

### Шаг 1: Настройте PostgreSQL (5 минут)

#### Вариант A: Neon (бесплатно, рекомендуется для теста)

1. Перейдите на https://neon.tech
2. Создайте аккаунт (GitHub/Google)
3. Создайте новый проект: "neoarchive"
4. Скопируйте **Connection string** (начинается с `postgresql://`)

**Готово!** У вас есть DATABASE_URL.

#### Вариант B: Railway ($5/месяц, полный стек)

1. Перейдите на https://railway.app
2. Создайте проект
3. Добавьте сервис: PostgreSQL
4. Скопируйте DATABASE_URL из Variables

**Бонус:** Позже развернете здесь же весь проект!

#### Вариант C: Supabase (только PostgreSQL, без API)

1. В вашем текущем Supabase проекте
2. Settings → Database → Connection string
3. Выберите "URI" режим
4. Скопируйте строку

**Примечание:** Используете только PostgreSQL, не Supabase API = без Egress лимитов!

---

### Шаг 2: Миграция данных (10 минут)

```bash
# 1. Экспортируйте данные из Supabase
export OLD_SUPABASE_URL="https://kovcgjtqbvmuzhsrcktd.supabase.co"
export OLD_SUPABASE_SERVICE_KEY="ваш-service-role-key"

npm run migrate:export

# Вы увидите:
# ✅ users: 15 записей экспортировано
# ✅ exhibits: 42 записей экспортировано
# ...
```

```bash
# 2. Создайте схему в новой БД
# Скопируйте содержимое schema-postgres.sql
# Вставьте в SQL Editor вашей новой БД (Neon/Railway/Supabase)
# Нажмите Run/Execute
```

```bash
# 3. Импортируйте данные
export DATABASE_URL="postgresql://user:password@host:5432/neoarchive"

npm install pg  # Установите PostgreSQL драйвер

npm run migrate:import:pg

# Вы увидите:
# ✅ users: 15 записей импортировано
# ✅ exhibits: 42 записей импортировано
# ...
```

---

### Шаг 3: Обновите код (5 минут)

```bash
# 1. Замените server.js на новую версию
mv server.js server-supabase.js.backup
mv server-postgres.js server.js

# 2. Создайте .env файл
cat > .env << EOF
NODE_ENV=production
PORT=3000
DATABASE_URL=ваш-connection-string-из-шага-1
EOF

# 3. Тестируйте локально
npm run build
npm start

# Откройте http://localhost:3000
```

**Проверьте:**
- ✅ Страница загружается
- ✅ Данные отображаются
- ✅ Можете создать новый экспонат
- ✅ В консоли: "✅ Работаем без Supabase!"

---

## 🚀 Деплой (выберите один вариант)

### Вариант A: Railway (самый простой)

```bash
# 1. Push код на GitHub
git add -A
git commit -m "Migrate to PostgreSQL"
git push

# 2. Railway:
# - Создайте новый проект
# - Connect GitHub репозиторий
# - Добавьте переменную DATABASE_URL (автоматически, если добавили PostgreSQL)
# - Deploy!

# Готово за 5 минут! ✅
```

### Вариант B: VPS (полный контроль)

```bash
# 1. Подключитесь к серверу
ssh root@your-vps-ip

# 2. Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# 3. Клонируйте проект
git clone https://github.com/your-repo/neoarchive.git
cd neoarchive

# 4. Настройте .env
nano .env
# DATABASE_URL=postgresql://...
# PORT=3000

# 5. Установите и соберите
npm install
npm run build

# 6. Запустите с PM2
npm install -g pm2
pm2 start server.js --name neoarchive
pm2 save
pm2 startup

# 7. Настройте Nginx (опционально)
# См. MIGRATION_OFF_SUPABASE.md
```

### Вариант C: Docker

```bash
# 1. Создайте образ
docker build -t neoarchive .

# 2. Запустите
docker run -d \
  -p 3000:3000 \
  -e DATABASE_URL="postgresql://..." \
  --name neoarchive \
  neoarchive

# Или с docker-compose:
docker-compose up -d
```

---

## 📋 Чеклист

### Перед миграцией:
- [ ] Создана новая PostgreSQL БД (Neon/Railway/другое)
- [ ] Получен CONNECTION_STRING
- [ ] Установлен драйвер: `npm install pg`

### Миграция данных:
- [ ] Экспортированы данные: `npm run migrate:export`
- [ ] Создана схема: выполнен `schema-postgres.sql`
- [ ] Импортированы данные: `npm run migrate:import:pg`
- [ ] Проверены данные в новой БД

### Обновление кода:
- [ ] Заменен `server.js` → `server-postgres.js`
- [ ] Создан `.env` с `DATABASE_URL`
- [ ] Протестировано локально: `npm start`
- [ ] Всё работает ✅

### Деплой:
- [ ] Выбран хостинг (Railway/VPS/Docker)
- [ ] Развернут проект
- [ ] Настроен домен (опционально)
- [ ] SSL сертификат (Let's Encrypt/Cloudflare)

### Cleanup:
- [ ] Удален старый Supabase проект (если не нужен)
- [ ] Удален `@supabase/supabase-js`: `npm uninstall @supabase/supabase-js`
- [ ] Обновлена документация

---

## 💰 Экономия

### Было (Supabase):
- **Free Plan:** Превышен Egress (314%)
- **Pro Plan:** $25/месяц минимум

### Стало:
- **Neon + Railway:** $5/месяц
- **VPS (Hetzner):** $5/месяц
- **Полный контроль:** Бесценно! 🎉

**Экономия:** $20-25/месяц = $240-300/год

---

## 🆘 Помощь

### Ошибка подключения к БД?

```bash
# Проверьте connection string:
echo $DATABASE_URL

# Должен быть формат:
# postgresql://user:password@host:5432/dbname

# Тест подключения:
node -e "import('pg').then(({default:pg})=>{const c=new pg.Client(process.env.DATABASE_URL);c.connect().then(()=>console.log('✅ OK')).catch(e=>console.error('❌',e.message))})"
```

### Данные не импортируются?

```bash
# Проверьте логи:
cat migration-data/_import-postgres-report.json

# Повторите импорт (безопасно):
npm run migrate:import:pg
```

### API не работает?

```bash
# Проверьте health endpoint:
curl http://localhost:3000/api/health

# Должен вернуть:
# {"status":"ok","database":"connected"}
```

---

## ✅ Готово!

Вы свободны от Supabase! 🎉

**Что дальше:**
1. Настройте резервное копирование БД
2. Настройте мониторинг (UptimeRobot бесплатно)
3. Настройте CDN для статики (Cloudflare бесплатно)
4. Наслаждайтесь отсутствием лимитов!

**Полная документация:** См. `MIGRATION_OFF_SUPABASE.md`

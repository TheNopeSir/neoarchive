# Игнорируем зависимости (они установятся внутри контейнера)
node_modules

# Игнорируем локальные сборки
dist

# Игнорируем локальную базу данных (чтобы не перезатирать данные на сервере при деплое)
data/*
!data/.keep

# Системные файлы и логи
.git
.gitignore
.DS_Store
npm-debug.log
yarn-debug.log
yarn-error.log

# Файлы Docker
Dockerfile
docker-compose.yml
.dockerignore

# Файлы окружения (если есть секреты, их лучше передавать через ENV переменные контейнера)
.env
.env.local
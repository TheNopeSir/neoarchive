# 🎯 Миграция NeoArchive с Supabase на NeoBD

## Шаг 1: Подготовка connection string

Из вашего скриншота:
```bash
POSTGRESQL_HOST=10485197b297c9ddd0cd3434.twc1.net
POSTGRESQL_PORT=5432
POSTGRESQL_USER=gen_user
POSTGRESQL_PASSWORD=******** # Ваш пароль
POSTGRESQL_DBNAME=default_db
```

**Connection string:**
```bash
export DATABASE_URL="postgresql://gen_user:ВАШ_ПАРОЛЬ@10485197b297c9ddd0cd3434.twc1.net:5432/default_db?sslmode=require"
```

⚠️ **Замените `ВАШ_ПАРОЛЬ` на реальный пароль из NeoBD!**

---

## Шаг 2: Экспорт данных из Supabase (1 раз)

```bash
# Установите переменные для Supabase
export OLD_SUPABASE_URL="https://kovcgjtqbvmuzhsrcktd.supabase.co"
export OLD_SUPABASE_SERVICE_KEY="ваш-service-role-ключ-из-supabase"

# Экспортируйте данные
npm run migrate:export
```

**Результат:**
```
📦 Экспорт таблицы: users
✅ users: 15 записей экспортировано

📦 Экспорт таблицы: exhibits
✅ exhibits: 42 записей экспортировано

📦 Экспорт таблицы: collections
✅ collections: 8 записей экспортировано

...

✅ Экспорт завершен! Данные сохранены в: ./migration-data
```

---

## Шаг 3: Создание схемы в NeoBD

### Вариант A: Через Web-интерфейс NeoBD

1. Откройте NeoBD → **SQL Editor** или **Query**
2. Скопируйте содержимое файла `schema-postgres.sql`
3. Вставьте в редактор
4. Нажмите **Выполнить** / **Execute**

### Вариант B: Через командную строку

```bash
# Установите psql (если еще не установлен)
# Ubuntu/Debian:
sudo apt-get install postgresql-client

# macOS:
brew install postgresql

# Выполните схему
export DATABASE_URL="postgresql://gen_user:ВАШ_ПАРОЛЬ@10485197b297c9ddd0cd3434.twc1.net:5432/default_db?sslmode=require"

psql $DATABASE_URL < schema-postgres.sql
```

**Проверьте создание таблиц:**
```bash
psql $DATABASE_URL -c "\dt"
```

Должны увидеть:
```
 users
 exhibits
 collections
 notifications
 messages
 guestbook
```

---

## Шаг 4: Импорт данных в NeoBD

```bash
# Установите pg драйвер (если еще не установлен)
npm install pg

# Установите connection string для NeoBD
export DATABASE_URL="postgresql://gen_user:ВАШ_ПАРОЛЬ@10485197b297c9ddd0cd3434.twc1.net:5432/default_db?sslmode=require"

# Запустите импорт
npm run migrate:import:pg
```

**Процесс импорта:**
```
🚀 Начинаем импорт в PostgreSQL...

✅ Подключение к PostgreSQL успешно

📋 Импорт данных от: 2024-12-16T10:30:00.000Z
📋 Всего записей для импорта: 115

📦 Импорт таблицы: users
  ⏳ Прогресс: 100% (15/15)
✅ users: 15 записей импортировано

📦 Импорт таблицы: exhibits
  ⏳ Прогресс: 100% (42/42)
✅ exhibits: 42 записей импортировано

...

✅ Импорт в PostgreSQL завершен!
```

---

## Шаг 5: Обновление кода

```bash
# 1. Сохраните старый server.js (на всякий случай)
cp server.js server-supabase.js.backup

# 2. Используйте новый server-postgres.js
cp server-postgres.js server.js

# 3. Создайте .env файл
cat > .env << EOF
NODE_ENV=production
PORT=3000
DATABASE_URL=postgresql://gen_user:ВАШ_ПАРОЛЬ@10485197b297c9ddd0cd3434.twc1.net:5432/default_db?sslmode=require
EOF

# ⚠️ ВАЖНО: Замените ВАШ_ПАРОЛЬ на реальный!
```

---

## Шаг 6: Тестирование

```bash
# 1. Соберите проект
npm run build

# 2. Запустите сервер
npm start
```

**Откройте:** http://localhost:3000

**Проверьте:**
- ✅ Страница загружается
- ✅ Отображаются экспонаты
- ✅ Работает авторизация
- ✅ Можно создать новый экспонат
- ✅ В консоли сервера: `✅ Работаем без Supabase!`

---

## Шаг 7: Деплой

После успешного теста:

```bash
# 1. Закоммитьте изменения
git add .env server.js
git commit -m "Migrate to NeoBD PostgreSQL"
git push

# 2. На сервере (VPS/Railway/Vercel):
# - Добавьте переменную окружения DATABASE_URL
# - Перезапустите приложение
```

---

## ⚠️ Важные моменты

### 1. Безопасность .env файла

```bash
# Добавьте .env в .gitignore (если еще не добавлен)
echo ".env" >> .gitignore

# НИКОГДА не коммитьте .env в git!
```

### 2. Проверка подключения

```bash
# Тест connection string:
psql "postgresql://gen_user:ВАШ_ПАРОЛЬ@10485197b297c9ddd0cd3434.twc1.net:5432/default_db?sslmode=require" -c "SELECT version();"

# Должна вернуться версия PostgreSQL
```

### 3. Проверка данных после импорта

```bash
# Подключитесь к БД
psql $DATABASE_URL

# Проверьте количество записей
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM exhibits;
SELECT COUNT(*) FROM collections;

# Проверьте данные
SELECT data->>'username' as username FROM users LIMIT 5;
SELECT data->>'title' as title FROM exhibits LIMIT 5;
```

---

## 🎉 После успешной миграции

### 1. Удалите Supabase зависимость

```bash
npm uninstall @supabase/supabase-js
```

### 2. Удалите старые файлы

```bash
rm server-supabase.js.backup  # Если больше не нужен
rm -rf migration-data          # Если импорт прошел успешно
```

### 3. Удалите Supabase проект

Если больше не нужен, удалите проект в Supabase Dashboard.

---

## 💰 Экономия

**Было:**
- Supabase: Превышен Egress (314%)
- Нужен Pro план: $25/месяц

**Стало:**
- NeoBD: Без лимитов на Egress
- Ваша текущая стоимость NeoBD
- **Экономия:** минимум $25/месяц = $300/год

---

## 🆘 Troubleshooting

### Ошибка: "password authentication failed"

**Решение:**
```bash
# Проверьте пароль в NeoBD интерфейсе
# Убедитесь, что в connection string нет специальных символов
# Если есть @ # % и т.д., используйте URL encoding:
# @ → %40
# # → %23
# % → %25
```

### Ошибка: "SSL connection required"

**Решение:**
```bash
# Добавьте ?sslmode=require в конец connection string:
DATABASE_URL="postgresql://...?sslmode=require"
```

### Ошибка: "relation does not exist"

**Решение:**
```bash
# Схема не создана, выполните schema-postgres.sql:
psql $DATABASE_URL < schema-postgres.sql
```

### Данные не отображаются после импорта

**Проверка:**
```bash
# Подключитесь к БД
psql $DATABASE_URL

# Проверьте есть ли данные
SELECT COUNT(*) FROM exhibits;

# Если 0, повторите импорт:
npm run migrate:import:pg
```

---

## ✅ Готово!

Теперь NeoArchive работает с вашей NeoBD базой данных!

**Преимущества:**
- ✅ Без лимитов Egress
- ✅ Полный контроль над данными
- ✅ Быстрее (прямые SQL запросы)
- ✅ Экономия $300/год

---

## 📋 Чеклист

- [ ] Получен пароль от NeoBD
- [ ] Создан connection string
- [ ] Экспортированы данные: `npm run migrate:export`
- [ ] Создана схема в NeoBD: выполнен `schema-postgres.sql`
- [ ] Импортированы данные: `npm run migrate:import:pg`
- [ ] Обновлен server.js
- [ ] Создан .env файл
- [ ] Протестировано локально
- [ ] Развернуто на продакшене
- [ ] Удален Supabase проект

**Нужна помощь на каком-то шаге?** Дайте знать! 🚀

version: '3.8'

services:
  neoarchive:
    build: .
    container_name: neoarchive_app
    ports:
      - "3000:3000"
    volumes:
      # Persist the database file
      - ./data:/app/data
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
